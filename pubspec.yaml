name: terceira_prova
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  floor: ^1.4.2
  connectivity_plus: ^5.0.2
  http: ^1.1.0
  sqflite: ^2.3.0
  path: ^1.8.3


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_launcher_icons: ^0.13.1
  floor_generator: ^1.4.2
  build_runner: ^2.1.2

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/poke_icon.png"
  min_sdk_android: 21

flutter:
  uses-material-design: true

